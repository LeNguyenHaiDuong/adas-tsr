name: adas-tsr
channels:
  - pytorch
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pytorch::pytorch
  - pytorch::torchvision
  - pytorch::cpuonly
  - scipy>=1.10.0
  - jupyterlab
  - ipykernel
  - nbformat
  - nbconvert
  - pip:
      - -r requirements.txt
